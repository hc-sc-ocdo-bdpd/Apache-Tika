# File-processing Document Processing Demo

## Quick Start with Docker

1. Clone this repository
2. Build the image:
```bash
   docker build -t file-processing .
```
3. Run it:
```bash
   docker run file-processing
```

## Requirements
- Docker Desktop installed
- That's it!